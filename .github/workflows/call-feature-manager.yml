name: trigger workflows

on:
  workflow_dispatch:
  
jobs:
  call-reusable-workflow:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: master issue workflow
      uses: ./.github/workflows/add-feature-to-feature-project.yml
